executor: slurm
max-jobs-per-second: 10
max-status-checks-per-second: 1
latency-wait: 5
jobs: 99  # Adjust as needed
default-resources:
    slurm_account: "system"
    slurm_partition: "fn_long"
    mem_mb_per_cpu: 16000 # take a sensible default for your cluster
    runtime: "120h"
    cores: 8
